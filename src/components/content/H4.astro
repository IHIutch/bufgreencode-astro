---
import { HeadingLink } from './HeadingLink'
const { id } = Astro.props
---

<div class="mt-[2em]">
  <HeadingLink id={id} client:only="react" />
  <h4 id={id} class="mt-0 scroll-mt-24">
    <slot />
  </h4>
</div>
